#include "main.h"
#include <stdio.h>

/**
 * _islower - checks for lower case letters
 * @c: character
 * Return: 0 on success
 */
int _islower(int c);
{
	if (c >= 'a'; &&c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
